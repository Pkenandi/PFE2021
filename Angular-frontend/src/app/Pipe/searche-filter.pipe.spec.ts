import { SearcheFilterPipe } from './searche-filter.pipe';

describe('SearcheFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearcheFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
